{
  "name": "coffee-qr-menu",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Coffee Shop QR Menu — mobile-first PWA (scan → landing → OTP → menu with vertical tabs → live order status)",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.4",
    "prisma": "^5.22.0"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "cors": "^2.8.6",
    "express": "^5.2.1"
  }
}
